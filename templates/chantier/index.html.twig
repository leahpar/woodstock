{% extends 'base.html.twig' %}

{% block title %}chantier index{% endblock %}

{% block content %}

    
    <div class="pageHeader sticky top-0 z-10 bg-gray-100 py-3 px-3 lg:px-0 -mx-4 lg:mx-0 space-y-2">
      <div class="title">
        <h2>Gestion des chantiers</h2>
      </div>
      <div class="buttons">

        {% if is_granted('ROLE_CHANTIER_EDIT') %}
            <a href="{{ path('chantier_new') }}" class="button">Ajouter un chantier</a>
        {% endif %}

      </div>
    </div>

    {{ include('chantier/_table.html.twig', { 'chantiers': chantiers }) }}
    {{ include('parts/pagination.html.twig', {'search': search}) }}

{% endblock %}
