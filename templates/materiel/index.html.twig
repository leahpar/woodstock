{% extends 'base.html.twig' %}

{% block title %}matériel index{% endblock %}

{% block content %}

    <div class="pageHeader sticky top-0 z-10 bg-gray-100 py-3 px-3 lg:px-0 -mx-4 lg:mx-0 space-y-2">
      <div class="title">
        <h2>Gestion du matériel</h2>
      </div>
      <div class="buttons">
        <a href="{{ path('materiel_new') }}" class="borderedButton">Ajouter un matériel</a>
      </div>
    </div>

    {{ include('parts/recherche2.html.twig', {search: search}) }}
    {{ include('materiel/_table.html.twig', { materiels: materiels }) }}
    {{ include('parts/pagination.html.twig', {'search': search}) }}

{% endblock %}
