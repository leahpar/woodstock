<html>
<head>
<title>Catalogue</title>
</head>
<body>

<h1>Catalogue des références</h1>
{% if categorie %}
<h2>{{ categorie }}</h2>
{% endif %}

<div style="
    display: flex;
    flex-flow: row wrap;
    justify-content: space-evenly;
">
{% for ref in references %}
<div style="width: 280px; height: 330px; text-align: center; ">
    <h2>{{ ref.nom }}</h2>
    <p>
        {{ ref.reference }}
        {{ ref.marque }}
        {{ ref.categorie }}
        {{ ref.conditionnement }}
    </p>
    <img src="{{ ref.reference|qrcode }}" width="200px" height="200px">
</div>
{% endfor %}
</div>

</body>
</html>
