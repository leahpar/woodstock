<nav>
    <ul>
        <li><a href="{{ path('dashboard') }}">Accueil</a></li>

        {% if is_granted('ROLE_USER_LIST') %}
        <li><a href="{{ path('user_index') }}">Utilisateurs</a></li>
        {% endif %}

        {% if is_granted('ROLE_REFERENCE_LIST') %}
        <li><a href="{{ path('reference_index') }}">Références</a></li>
        {% endif %}

        {% if is_granted('ROLE_REFERENCE_STOCK') %}
        <li><a href="{{ path('reference_stock') }}">Nouvelle entrée</a></li>
        {% endif %}

        <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
    </ul>
</nav>
